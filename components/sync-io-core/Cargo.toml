[package]
name = "sync-io-core"
version = "0.0.0"
edition = "2021"
publish = false
[lib]
crate-type = ["rlib"]
[dependencies]
trantor-abi = "0.0.0"      # generated; the consumer's workspace patches it
