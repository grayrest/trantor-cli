[package]
name = "sync-io"
version = "0.0.0"
edition = "2021"
publish = false
[lib]
crate-type = ["staticlib", "rlib"]
[dependencies]
trantor-abi = "0.0.0"      # generated; the consumer's workspace patches it
sync-io-core = { path = "../sync-io-core" }
# F_DUPFD_CLOEXEC and close, for FdHandoff.
libc = "=0.2.189"
